# Server.dart Code Fix TODO

## Tasks
- [ ] Mount ApiRoutes router at '/api' instead of defining routes directly
- [ ] Remove duplicate route definitions from main router
- [ ] Keep existing mounted routers (scrf, student, admin)
- [ ] Test that all routes still work after changes

## Changes Made
- Mounted ApiRoutes router
- Removed duplicate route definitions
